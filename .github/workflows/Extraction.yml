name: Manual SusFS File Extraction and Commit

on:
  workflow_dispatch:
    inputs:
      source_commits:
        description: "Source commit hashes (short or full, comma separated)"
        required: true
        default: "41273ce"
      source_branch:
        description: "Source branch to get commits from"
        required: true
        default: "android_kernel_common_android12-5.10"
      target_branch:
        description: "Target branch to push to"
        required: true
        default: "miuix"

env:
  SOURCE_REPO: "sqldown/android_kernel_common_android12-5.10"
  TARGET_REPO: "mozi9/XCD"
  COMMIT_MESSAGE: "fs: Bump SuSFS version to v2.0.0"
  COMMIT_DESCRIPTION: "Co-authored-by: simonpunk <simonpunk2016@gmail.com> o-authored-by: JackA1ltman <cs2dtzq@163.com>"

jobs:
  extract-and-commit:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout source repository
      uses: actions/checkout@v4
      with:
        repository: ${{ env.SOURCE_REPO }}
        ref: ${{ inputs.source_branch }}
        token: ${{ secrets.Mozi9_PAT }}
        path: source-repo
        fetch-depth: 0

    - name: Checkout target repository
      uses: actions/checkout@v4
      with:
        repository: ${{ env.TARGET_REPO }}
        ref: ${{ inputs.target_branch }}
        token: ${{ secrets.Mozi9_PAT }}
        path: target-repo
        fetch-depth: 0

    - name: Setup Git identity (mozi9)
      run: |
        git config --global user.name "mozi9"
        git config --global user.email "mozi9"
        echo "Git configured: mozi9 <mozi9>"

    - name: Resolve commit hashes
      id: resolve-commits
      run: |
        cd source-repo
        COMMITS=$(echo "${{ inputs.source_commits }}" | tr ',' '\n' | sed 's/^[[:space:]]*//;s/[[:space:]]*$//' | grep -v '^$')
        RESOLVED_COMMITS=""
        
        for commit in $COMMITS; do
          FULL_HASH=$(git rev-parse "$commit" 2>/dev/null || echo "")
          if [ -n "$FULL_HASH" ]; then
            RESOLVED_COMMITS="$RESOLVED_COMMITS$FULL_HASH"$'\n'
          else
            echo "❌ Error: Cannot resolve commit $commit"
            exit 1
          fi
        done
        
        echo "$RESOLVED_COMMITS" | grep -v '^$' > ../resolved_commits.txt
        echo "✅ Resolved commits: $(cat ../resolved_commits.txt | tr '\n' ' ')"

    - name: Analyze file changes
      id: analyze-files
      run: |
        cd source-repo
        COMMITS=$(cat ../resolved_commits.txt)
        
        ALL_MODIFIED_FILES=""
        ALL_DELETED_FILES=""
        
        echo "=== Analyzing file changes ==="
        
        for commit in $COMMITS; do
          echo "Processing commit: $commit"
          
          MODIFIED_FILES=$(git show --name-status --format= $commit | awk '$1 == "A" || $1 == "M" {print $2}' | sort -u)
          DELETED_FILES=$(git show --name-status --format= $commit | awk '$1 == "D" {print $2}' | sort -u)
          
          if [ -n "$MODIFIED_FILES" ]; then
            ALL_MODIFIED_FILES="$ALL_MODIFIED_FILES"$'\n'"$MODIFIED_FILES"
          fi
          if [ -n "$DELETED_FILES" ]; then
            ALL_DELETED_FILES="$ALL_DELETED_FILES"$'\n'"$DELETED_FILES"
          fi
        done
        
        # 去重文件列表
        UNIQUE_MODIFIED=$(echo "$ALL_MODIFIED_FILES" | grep -v '^$' | sort -u)
        UNIQUE_DELETED=$(echo "$ALL_DELETED_FILES" | grep -v '^$' | sort -u)
        
        echo "$UNIQUE_MODIFIED" > ../modified_files.txt
        echo "$UNIQUE_DELETED" > ../deleted_files.txt
        
        MOD_COUNT=$(echo "$UNIQUE_MODIFIED" | grep -c . || echo 0)
        DEL_COUNT=$(echo "$UNIQUE_DELETED" | grep -c . || echo 0)
        
        echo "modified_files_count=$MOD_COUNT" >> $GITHUB_OUTPUT
        echo "deleted_files_count=$DEL_COUNT" >> $GITHUB_OUTPUT
        
        echo "✅ Modified files: $MOD_COUNT"
        echo "✅ Deleted files: $DEL_COUNT"

    - name: Delete target files first
      id: delete-files
      run: |
        cd target-repo
        
        echo "=== Deleting files from target ==="
        DELETED_COUNT=0
        
        if [ -s ../deleted_files.txt ]; then
          while IFS= read -r file; do
            if [ -n "$file" ] && [ -f "$file" ]; then
              # 安全检查
              if [[ "$file" != / && "$file" != /* && "$file" != .git* && "$file" != "." && "$file" != ".." ]]; then
                echo "🗑️ Deleting: $file"
                rm -f "$file"
                DELETED_COUNT=$((DELETED_COUNT + 1))
              else
                echo "🚫 Skipped (safety): $file"
              fi
            fi
          done < ../deleted_files.txt
        fi
        
        echo "actual_deleted_count=$DELETED_COUNT" >> $GITHUB_OUTPUT
        echo "✅ Deleted $DELETED_COUNT files"

    - name: Copy modified files to target
      id: copy-files
      run: |
        cd source-repo
        git checkout ${{ inputs.source_branch }} --quiet
        
        echo "=== Copying modified files ==="
        COPIED_COUNT=0
        
        if [ -s ../modified_files.txt ]; then
          while IFS= read -r file; do
            if [ -n "$file" ] && [ -f "$file" ]; then
              echo "📄 Copying: $file"
              mkdir -p "../target-repo/$(dirname "$file")"
              cp "$file" "../target-repo/$file"
              COPIED_COUNT=$((COPIED_COUNT + 1))
            fi
          done < ../modified_files.txt
        fi
        
        echo "actual_copied_count=$COPIED_COUNT" >> $GITHUB_OUTPUT
        echo "✅ Copied $COPIED_COUNT files"

    - name: Verify file operations
      id: verify-files
      run: |
        echo "=== Verification ==="
        echo "Expected to copy: $(cat modified_files.txt | wc -l) files"
        echo "Expected to delete: $(cat deleted_files.txt | wc -l) files"
        echo "Actually copied: ${{ steps.copy-files.outputs.actual_copied_count }} files"
        echo "Actually deleted: ${{ steps.delete-files.outputs.actual_deleted_count }} files"
        
        cd target-repo
        echo "Files in target repo: $(find . -type f -not -path '*/\.git/*' | wc -l)"
        echo "Git status:"
        git status --short

    - name: Commit changes (simple message)
      id: commit-changes
      run: |
        cd target-repo
        
        # 检查变更
        git add -A
        if git diff --cached --quiet; then
          echo "❌ No changes to commit"
          echo "has_changes=false" >> $GITHUB_OUTPUT
          # 调试信息
          echo "=== Debug info ==="
          git status
          find . -type f -not -path '*/\.git/*' | head -10
          exit 1
        else
          echo "has_changes=true" >> $GITHUB_OUTPUT
          echo "=== Files to commit ==="
          git diff --cached --name-only
          
          # 简单提交信息
          git commit -m "${{ env.COMMIT_MESSAGE }}" -m "${{ env.COMMIT_DESCRIPTION }}"
          echo "✅ Commit created"
        fi

    - name: Push to target repository
      if: steps.commit-changes.outputs.has_changes == 'true'
      run: |
        cd target-repo
        git push origin ${{ inputs.target_branch }}
        echo "✅ Pushed to ${{ env.TARGET_REPO }}:${{ inputs.target_branch }}"

    - name: Show final result
      run: |
        cat > $GITHUB_STEP_SUMMARY << EOF
        # 文件处理结果
        
        ## 操作统计
        - **分析出的修改文件**: ${{ steps.analyze-files.outputs.modified_files_count }} 个
        - **分析出的删除文件**: ${{ steps.analyze-files.outputs.deleted_files_count }} 个
        - **实际复制的文件**: ${{ steps.copy-files.outputs.actual_copied_count }} 个
        - **实际删除的文件**: ${{ steps.delete-files.outputs.actual_deleted_count }} 个
        
        ## Git配置
        - **作者**: mozi9
        - **邮箱**: mozi9
        
        ## 提交信息
        - **标题**: ${{ env.COMMIT_MESSAGE }}
        - **描述**: ${{ env.COMMIT_DESCRIPTION }}
        
        EOF
        
        if [ "${{ steps.commit-changes.outputs.has_changes }}" = "true" ]; then
          echo "✅ **成功**: 正确处理了 ${{ steps.copy-files.outputs.actual_copied_count }} 个文件" >> $GITHUB_STEP_SUMMARY
        else
          echo "❌ **失败**: 没有检测到文件变更" >> $GITHUB_STEP_SUMMARY
        fi
