name: Source Commit Sync - Force Source Files

on:
  workflow_dispatch:
    inputs:
      source_repo:
        description: '源仓库地址'
        required: true
        default: 'owner/source-repo'
      source_branch:
        description: '源分支名'
        required: true
        default: 'susfs-2.0.0'
      target_repo:
        description: '目标仓库地址'
        required: true
        default: 'owner/target-repo'
      target_branch:
        description: '目标分支名'
        required: true
        default: 'MIUI'
      commit_hashes:
        description: '要同步的提交哈希'
        required: true
        default: '32ac13d'
      new_branch_name:
        description: '新分支名（留空推送到目标分支）'
        required: false
        default: ''

env:
  GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

jobs:
  force-source-files:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout target repository
      uses: actions/checkout@v4
      with:
        repository: ${{ github.event.inputs.target_repo }}
        token: ${{ secrets.Mozi9_PAT }}
        ref: ${{ github.event.inputs.target_branch }}
        fetch-depth: 0

    - name: Setup Git identity
      run: |
        git config user.name "mozi9"
        git config user.email "mozi9"

    - name: Add source repository
      run: |
        git remote add source https://github.com/${{ github.event.inputs.source_repo }}.git
        git fetch source ${{ github.event.inputs.source_branch }}

    - name: Create or select target branch
      id: setup_branch
      run: |
        if [ -z "${{ github.event.inputs.new_branch_name }}" ]; then
          TARGET_BRANCH="${{ github.event.inputs.target_branch }}"
          echo "使用现有分支: $TARGET_BRANCH"
        else
          TARGET_BRANCH="${{ github.event.inputs.new_branch_name }}"
          echo "创建新分支: $TARGET_BRANCH"
          git checkout -b "$TARGET_BRANCH"
        fi
        echo "target_branch=$TARGET_BRANCH" >> $GITHUB_OUTPUT

    - name: Apply commits with file copy
      id: apply_commits
      run: |
        echo "应用提交（文件复制方式）..."
        COMMITS=$(echo '${{ github.event.inputs.commit_hashes }}' | tr ',' ' ')
        APPLIED_COUNT=0
        
        for COMMIT in $COMMITS; do
          echo "处理提交: $COMMIT"
          
          # 获取提交信息
          ORIG_MSG=$(git log -1 --format=%B "$COMMIT")
          ORIG_DATE=$(git log -1 --format="%ci" "$COMMIT")
          
          # 清理作者信息
          CLEAN_MSG=$(echo "$ORIG_MSG" | grep -v -E "(Co-authored-by|Reviewed-by|Signed-off-by):")
          
          # 获取提交中的所有文件
          FILES=$(git show --name-only --format= "$COMMIT" | grep -v '^$')
          echo "提交包含文件: $(echo "$FILES" | wc -l) 个"
          
          # 强制复制所有源文件
          for file in $FILES; do
            echo "复制文件: $file"
            mkdir -p "$(dirname "$file")"
            git show "$COMMIT":"$file" > "$file" 2>/dev/null || echo "⚠️ 文件不存在: $file"
          done
          
          # 处理删除的文件
          DELETED_FILES=$(git show --name-only --diff-filter=D "$COMMIT" 2>/dev/null || true)
          if [ -n "$DELETED_FILES" ]; then
            echo "删除文件: $(echo "$DELETED_FILES" | wc -l) 个"
            for dfile in $DELETED_FILES; do
              rm -f "$dfile" 2>/dev/null || true
            done
          fi
          
          # 提交
          git add -A
          git commit -m "$CLEAN_MSG" --author="mozi9 <mozi9>" --date="$ORIG_DATE"
          
          APPLIED_COUNT=$((APPLIED_COUNT + 1))
          echo "✅ 提交应用完成"
        done
        
        echo "applied_count=$APPLIED_COUNT" >> $GITHUB_OUTPUT

    - name: Push to target branch
      run: |
        git push origin ${{ steps.setup_branch.outputs.target_branch }}
        echo "✅ 推送完成"

    - name: Generate final report
      run: |
        echo ""
        echo "🎉 源提交同步完成!"
        echo "=========================================="
        echo "📊 执行结果:"
        echo "  源仓库: ${{ github.event.inputs.source_repo }}"
        echo "  源分支: ${{ github.event.inputs.source_branch }}"
        echo "  目标仓库: ${{ github.event.inputs.target_repo }}"
        echo "  目标分支: ${{ steps.setup_branch.outputs.target_branch }}"
        echo "  处理提交: ${{ steps.apply_commits.outputs.applied_count }} 个"
        echo ""
        echo "✅ 文件处理策略:"
        echo "  - 所有文件强制使用源版本"
        echo "  - 忽略目标仓库的修改"
        echo "  - 完全保持源提交内容"
        echo ""
        echo "✅ 作者信息处理:"
        echo "  - 所有作者统一为 mozi9 <mozi9>"
        echo "  - 移除协作作者信息"
        echo "  - 保留原始提交时间"
        echo ""
        echo "👤 操作身份: mozi9 <mozi9>"
        echo "=========================================="
