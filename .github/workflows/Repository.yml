name: Source Commit Sync - Preserve Target History

on:
  workflow_dispatch:
    inputs:
      source_repo:
        description: '源仓库地址'
        required: true
        default: 'owner/source-repo'
      target_repo:
        description: '目标仓库地址'
        required: true
        default: 'owner/target-repo'
      commit_hashes:
        description: '要应用的提交哈希'
        required: true
        default: ''
      new_branch_name:
        description: '新分支名'
        required: true
        default: 'sync-source-commits'
      commit_message:
        description: '合并提交信息'
        required: true
        default: 'sync: 同步源提交'

env:
  GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

jobs:
  sync-preserve-history:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout target repository
      uses: actions/checkout@v4
      with:
        repository: ${{ github.event.inputs.target_repo }}
        token: ${{ secrets.Mozi9_PAT }}
        ref: 'main'
        fetch-depth: 0

    - name: Setup Git identity
      run: |
        git config --global user.name "mozi9"
        git config --global user.email "mozi9"

    - name: Add source repository
      run: |
        git remote add source https://github.com/${{ github.event.inputs.source_repo }}.git
        git fetch source

    - name: Create new branch from target
      id: create_branch
      run: |
        BRANCH_NAME="${{ github.event.inputs.new_branch_name }}"
        git checkout -b "$BRANCH_NAME"
        echo "branch_name=$BRANCH_NAME" >> $GITHUB_OUTPUT
        echo "✅ 创建分支: $BRANCH_NAME"

    - name: Apply source commits with file copy
      id: apply_commits
      run: |
        echo "应用源提交（文件复制方式）..."
        COMMITS=$(echo '${{ github.event.inputs.commit_hashes }}' | tr ',' ' ')
        APPLIED_COUNT=0
        
        for COMMIT in $COMMITS; do
          echo "处理提交: $COMMIT"
          
          # 获取提交信息
          ORIG_MSG=$(git log -1 --format=%B "$COMMIT")
          ORIG_DATE=$(git log -1 --format="%ci" "$COMMIT")
          
          # 清理作者信息
          CLEAN_MSG=$(echo "$ORIG_MSG" | grep -v -E "(Co-authored-by|Reviewed-by|Signed-off-by):")
          
          # 获取提交中的所有文件
          FILES=$(git show --name-only --format= "$COMMIT" | grep -v '^$')
          echo "提交包含文件: $(echo "$FILES" | wc -l) 个"
          
          # 复制所有源文件
          for file in $FILES; do
            echo "复制: $file"
            mkdir -p "$(dirname "$file")"
            git show "$COMMIT":"$file" > "$file" 2>/dev/null || echo "⚠️ 文件不存在: $file"
          done
          
          # 处理删除的文件
          DELETED_FILES=$(git show --name-only --diff-filter=D "$COMMIT" 2>/dev/null || true)
          if [ -n "$DELETED_FILES" ]; then
            for dfile in $DELETED_FILES; do
              rm -f "$dfile" 2>/dev/null || true
            done
          fi
          
          # 提交
          git add -A
          git commit -m "$CLEAN_MSG" --author="mozi9 <mozi9>" --date="$ORIG_DATE"
          
          APPLIED_COUNT=$((APPLIED_COUNT + 1))
          echo "✅ 提交应用完成"
        done
        
        echo "applied_count=$APPLIED_COUNT" >> $GITHUB_OUTPUT

    - name: Push new branch only
      id: push_branch
      run: |
        BRANCH_NAME="${{ steps.create_branch.outputs.branch_name }}"
        echo "推送新分支: $BRANCH_NAME"
        git push -u origin "$BRANCH_NAME"
        echo "✅ 分支已推送"
        echo "pushed_branch=$BRANCH_NAME" >> $GITHUB_OUTPUT

    - name: Create pull request (可选)
      id: create_pr
      uses: actions/github-script@v6
      with:
        script: |
          const branchName = '${{ steps.push_branch.outputs.pushed_branch }}';
          const commitMessage = '${{ github.event.inputs.commit_message }}';
          
          await github.rest.pulls.create({
            owner: context.repo.owner,
            repo: context.repo.repo,
            title: commitMessage,
            head: branchName,
            base: 'main',
            body: '自动创建的同步请求\n\n源提交: ${{ github.event.inputs.commit_hashes }}'
          });

    - name: Final report
      run: |
        echo ""
        echo "🎉 源提交同步完成!"
        echo "=========================================="
        echo "📊 执行结果:"
        echo "  目标仓库: ${{ github.event.inputs.target_repo }}"
        echo "  新分支: ${{ steps.push_branch.outputs.pushed_branch }}"
        echo "  应用提交: ${{ steps.apply_commits.outputs.applied_count }} 个"
        echo "  主分支: 未受影响"
        echo ""
        echo "✅ 目标仓库历史保护:"
        echo "  - 主分支历史完整保留"
        echo "  - 所有更改在新分支进行"
        echo "  - 可手动审查后合并"
        echo ""
        echo "✅ 提交处理:"
        echo "  - 所有文件使用源版本"
        echo "  - 作者统一为 mozi9"
        echo "  - 提交时间戳保留"
        echo ""
        echo "🔗 下一步操作:"
        echo "  1. 审查新分支的更改"
        echo "  2. 手动合并到主分支（如需）"
        echo "  3. 或等待PR审查合并"
        echo "=========================================="
